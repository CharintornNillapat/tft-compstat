allowBuilds:
  sharp: false
  unrs-resolver: false
  # Supabase CLI downloads its native binary in postinstall.
  supabase: true
  # tsx's esbuild ships its binary as an optional platform package; the postinstall only verifies it.
  esbuild: false
